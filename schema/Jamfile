SubDir TOP schema ;


rule SchemaFile {
    local s ;
    s = $(<) ;
    SEARCH on $(s) = $(SEARCH_SOURCE) ;

    InstallFile $(INSTALLROOT)$(LIBDIR) : $(<) ;
}

SchemaFile field-names ;
SchemaFile flag-names ;
SchemaFile schema.pg ;
