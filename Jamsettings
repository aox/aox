# This file contains compile-time configuration variables. Most of
# these are compiled into the binaries. A few may be overridden at
# runtime using mailstore.conf.
#
# These variables may be overwritten using the environment, e.g.
#    PREFIX=$HOME/tmp/mailstore jam install
# to install in your home directory.
#
# Remember the spaces around '=' and before ';'.


# Files and directories. The directory names MUST NOT end with /.

INSTALLROOT = "" ;

PREFIX = /usr/local/mailstore ;
BINDIR = $(PREFIX)/bin ;
MANDIR = $(PREFIX)/man ;
LIBDIR = $(PREFIX)/lib ;
PIDFILEDIR = /var/run ;
JAILDIR = $(PREFIX)/jail ;
CONFIGDIR = $(PREFIX) ;
LOGFILE = $(PREFIX)/logfile ;

# the user and group needed.

ORYXUSER = "nobody" ;
ORYXGROUP = "nobody" ;

# How to compile

C++FLAGS = -g -Werror -funsigned-char ;
LINKFLAGS = -g ;


# Mode for installed binaries

EXEMODE = 555 ;


# Uncomment the next line to use a cryptlib from the system. The
# default is to use the one bundled with the mailstore source.
# USE_SYSTEM_CRYPTLIB = yes ;
# You may also need to add/change something in tlsproxy/Jamfile.


## Database settings.

# The Unix username of the PostgreSQL superuser. You need to set this if
# your superuser's username is not "postgres".
#
#PGUSER = pgsql ;

# The directory in which psql and pg_ctl live. You can ignore this
# setting in most cases, because psql and pg_ctl are usually in the
# PGUSER's PATH.
#
#PGBIN = /opt/postgres/bin ;

# The address of the Postgres server. This corresponds to the db-address
# variable in mailstore.conf, and may be the fully-qualified Unix socket
# path, or an IPv4/6 address.

DBADDRESS = /tmp/.s.PGSQL.5432 ;

# A Postgres user and database for Mailstore to use. If necessary, these
# are created by BINDIR/installer. To use an existing user/database,
# change these values and set DBPASS below. (Note: DBUSER must own the
# DBNAME database.)
#
# A password is generated during installation for a new DBUSER, so you
# don't need to set DBPASS unless you want to use an existing DBUSER.
#
# These settings correspond to the db-name, db-user, and db-password
# variables in mailstore.conf.

DBNAME = mailstore ;
DBUSER = oryx ;
#DBPASS = foo ;


# this file is included from Jamrules.
